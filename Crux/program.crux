class Counter {
	Counter(initialValue) {
		this.value = initialValue;
	}

	Add() {
		this.value += 1;
	}

	Subtract() {
		this.value -= 1;
	}

	Get() {
		return this.value;
	}
}

var counter = Counter(0);

counter.Add();
counter.Add();
counter.Add();
counter.Subtract();

print(counter.Get());